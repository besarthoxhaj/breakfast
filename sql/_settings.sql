/**
 * Settings.
 * 1) will disable (or enable) the pretty mode. The pretty mode is useful
 * when dealing with one big row, not that useful when dealing with many
 * short rows.
 */
-- \x on;
\x off;